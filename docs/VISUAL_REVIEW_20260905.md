# Проверка интерфейса Fluent 2026 R1 по скриншотам

Проверка выполнена пользователем в Fluent Student; наблюдения основаны на присланных скриншотах. Для каждого прохода ниже отдельно указана установленная сборка.

## Пакет переводов

Обработаны все 99 извлечённых записей Colormap, Adjacency и HybridInitialization: 81 translated, 9 needs_context и 9 do_not_translate. Неоднозначные записи имеют пустой перевод и не включаются в сборку. Предлагаемые варианты и причины сохранены в каталоге.

Используются компактные подписи без цепочек групп исходного ключа. Исходные ключи сохранены. Идентификаторы компонентов, фаз и управляющие символы не переводятся.

В глоссарий добавлены Cell Zone, Face Zone, Adjacent Face Zones, Turbulence Intensity, Velocity Magnitude и Hybrid Initialization. Удалены дубли ключей. Для Line Width сохранён ранее действовавший последний вариант «Толщина линии».

## Ограничения наблюдений

- На скриншотах видны завершение 20 итераций, карта давления и легенда векторов. Это проверка части интерфейса, а не подтверждение корректности физической модели или сходимости.
- sparger_inlet имеет тип Wall; у fluid_mrf выключен Frame Motion. Имена зон сами по себе не подтверждают заданные вход и вращение.
- Нулевые показания легенд не устанавливают причину нулевого поля. Предыдущее объяснение только отсутствием перепада давления было недостаточно обосновано.
- Сообщение о недопустимом объекте контуров не доказывает, что внутренние зоны вообще нельзя отображать.
- Инициализация и расчёт изменили данные сеанса. Было ли выполнено сохранение на диск, по скриншотам неизвестно.
- Английские подписи могли относиться к ранее установленному пакету: наличие строки в новом каталоге ещё не подтверждает её отображение в программе.

## Следующая визуальная проверка

После отдельной установки сборки проверить Colormap, Adjacency и HybridInitialization: компактность подписей, вкладки, сохранение исходных имён объектов и отсутствие цепочек групп. Остались английские подписи Fluid, Wall, навигационного дерева и динамических заголовков графики.

## Проход после установки `release-5009` (2026-09-07)

### Идентификация установленного пакета

- Fluent был закрыт перед установкой.
- Собран текущий каталог `translations/v2026R1/catalog.json`: 134 модуля `.qm`.
- Пакет установлен с резервной копией `backups/release-5009/install-manifest.json`.
- SHA-256 совпали у всех 134 установленных файлов; 126 прежних русских файлов сохранены в резервной копии.
- Fluent запущен с дочерней переменной `lang=ru`.
- Для прохода загружена только сетка `samples/Bioreactor-Mixing-Tank/run-bioreactor-mixing-tank.msh.gz`; файл case или data не загружался.

### Проверено визуально в установленном пакете

| Окно Fluent | Наблюдаемый результат |
| --- | --- |
| Главная страница | Основные подписи страницы «Общие» и кнопки сетки отображаются по-русски. |
| `Physics → Viscous…` | Основные группы, параметры k-omega, флажки и кнопки переведены. Окно только просмотрено. |
| `Physics → Cell Zones` и зона `fluid_mrf` | Заголовки и базовые параметры переведены; имена зон и материал `air` сохранены. |
| `Physics → Boundaries` и `sparger_inlet` | Страница и список зон переведены; диалог открыт как `Wall`, без применения изменений. |
| `Results → Graphics` | Заголовки, кнопки и настройки страницы переведены. |
| `Results → Surfaces` | Заголовок, фильтр и базовые команды переведены; объекты не изменялись. |
| `Domain → Mesh…` | Окно «Интерфейсы сетки» переведено по основным заголовкам, фильтрам и командам. |
| `Domain → Dynamic Mesh…` | Основные подписи переведены; флажок включения динамической сетки не менялся. |
| `Motion Definitions` | Окно открылось с пустым списком; русифицированы часть команд. |
| `Reference Frames` | Основные подписи и кнопки переведены; значения не менялись. |
| `Curvilinear Coordinate System` | Основные параметры и команды переведены; имена объектов и зон сохранены. |
| `Solution → Methods`, `Controls`, `Initialization` | Основные заголовки, параметры и команды переведены. Инициализация не запускалась. |

### Подтверждённые английские строки и проблемы отображения

- Верхняя лента и дерево навигации: большинство пунктов, включая `Physics`, `Results`, `Setup`, `Solution` и `Graphics`.
- `Physics → Viscous…`: технические обозначения `Alpha*_inf`, `Beta*_inf`, `TKE`, `Prandtl #`, а также значения `correlation`, `none`.
- Диалог `Fluid`: `Fluid`, `Rotation-Axis Origin`, вкладка `Multiphase`.
- `Boundary Conditions`: значение `mixture`; в диалоге `Wall` — заголовок `Wall` и вкладка `Structure`.
- В диалоге `Wall` и на странице граничных условий отображаются полные цепочки ключей, например `Стенка/Количество движения/...` и `Граничные условия/...`, вместо коротких подписей.
- `Results → Graphics`: `Mesh`, `Contours`, `Vectors`, `Pathlines`, `Particle Tracks`, `LICs`, `Sweep Surface`, `Scene Animation`, `Playback`, `Unavailable`.
- `Results → Surfaces`: `Fluid`, `Internal`, `Iso-surface`, `Point-surface`, `Wall`, `Surface Type`, `Points`, `0D/1D/2D Facets`, `Rendering Priority`, `Medium`, `Group`, `UnGroup`.
- `Mesh Interfaces`: `Fluid`, `Wall`, `Options…`.
- `Quality`: `Evaluate Mesh Quality…`, `Improve Mesh Quality…`.
- `Dynamic Mesh`: `In-Cylinder` и полные цепочки `Динамическая сетка/...` на части кнопок.
- `Motion Definitions`: `Manage Motion Definitions`, `Motion Definitions`, `New…`.
- `Reference Frames`: `Reference Frame`, `global`, `direction`.
- `Curvilinear Coordinate System`: `Curvilinear Coordinate System`, `Select Cell Zones`, `Cell Zones`, `Diffusion`, `Start and End`, `Display Face Zones`, `Visualize Direction Vectors`, `Direction`, `Auto Scale`, `Draw Mesh`, `Skip`.
- `Solution → Methods`: `Coupled`, `Rhie-Chow: momentum based`, `Least Squares Cell Based`, `Second Order`, `Second Order Upwind`, `Global Time Step`.

### Границы прохода

- Открытие окон и выбор зон не меняли граничные условия, материалы, параметры моделей или пользовательский case.
- Не запускались инициализация, расчёт, адаптация сетки, создание интерфейсов и сохранение case.
- Недоступность отдельных пунктов и пустой список определений движения зафиксированы только как наблюдаемое состояние; причина не устанавливалась.
- Проверка не подтверждает физическую корректность модели биореактора.

## Дополнительный проход `release-5009` (2026-09-10)

Ниже зафиксированы только результаты просмотра интерфейса по новым скриншотам. Для прохода использовалась загруженная сетка; параметры модели, материалы, границы, инициализация и расчёт не изменялись.

### Подтверждено

| Окно Fluent | Наблюдаемый результат |
| --- | --- |
| `Solution → Methods` | Все рабочие подписи переведены. Названия алгоритмов (`Coupled`, `Rhie-Chow`, `Second Order Upwind`) сохранены как технические имена. |
| `Solution → Controls` | Все видимые рабочие подписи и кнопки переведены. |
| `Solution → Initialization` | Все видимые подписи и команды переведены; инициализация не запускалась. |
| `Reference Values` | Страница и её параметры переведены, значения не менялись. |
| `Monitors` | Описание страницы переведено и читаемо. |
| `Report Definitions` | Заголовки, свойства и команды переведены; определения отчётов не создавались. |

### Зафиксированные замечания

- `Materials`: заголовки категорий `Fluid` и `Solid` остались английскими. В `Create/Edit Materials` большинство подписей ещё на английском; имена материалов `air` и `aluminum` намеренно не переводились.
- `Multiphase Model`: заголовок окна и `Volume Fraction Parameters` не переведены. Два варианта моделирования интерфейса отображены как «Дисперсная»; второй вариант требует исправления по исходному контексту.
- `Species Model`: заголовок окна английский, но видимые варианты модели и команды переведены.
- `Energy`: `Energy`, заголовок группы и `Energy Equation` остаются английскими.
- `Viscous Model`: заголовок окна и подраздел `k-omega Model` английские; обозначения, названия моделей и параметры сохранены намеренно.
- `Run Calculation`: английские значения `Automatic`, `Conservative` и `Current Session`.
- `Reports`, `Graphics and Animations`, `Plots`: названия типов элементов и маркеры `Unavailable` остаются английскими.
- `Surfaces`: не переведены системные категории и подписи отрисовки; пользовательские имена зон сохранены.
- `Boundary Conditions`/`Wall`: заголовок `Wall`, вкладка `Structure` и полные цепочки ключей на части элементов требуют исправления.
- `Mesh Interfaces`: `Fluid`, `Wall`, `Edit`, `List`, `Delete`, `Display`, `Options` остаются английскими.
- `Expression Manager`, `Manage Cell Registers`, `Manage Adaption Criteria`: в каждом окне остаётся значительная часть английских заголовков и команд.
- `Calculation Activities`: подпункты дерева и строки статуса остаются английскими.
- `Dynamic Mesh`: `In-Cylinder` и полные пути в подписях кнопок остаются видимыми.

Все статусы и конкретные замечания синхронизированы с `reviews/v2026R1/windows.yml`.

## Проход после установки `release-3009`

### Идентификация установленного пакета

- Собрана текущая версия каталога `translations/v2026R1/catalog.json`: 126 модулей.
- Fluent перед установкой был закрыт.
- Пакет установлен в локальную Fluent Student 2026 R1 с резервной копией: `backups/release-3009/install-manifest.json`.
- После установки совпали SHA-256 всех 126 файлов из манифеста сборки; прежние версии 58 заменённых файлов сохранены в резервной копии.
- Fluent был запущен через Launcher с дочерней переменной `lang=ru`.
- Для визуального прохода загружена только сетка `samples/Bioreactor-Mixing-Tank/run-bioreactor-mixing-tank.msh.gz`. В консоли Fluent отобразились зоны и `Done`.

### Что проверено визуально в установленном пакете

| Окно Fluent | Наблюдаемый результат |
| --- | --- |
| `Domain → Display…` / «Отображение сетки» | Русские подписи параметров, типов рёбер, прозрачности и команд отображения. |
| `Solution → Methods` | Переведены заголовки и подписи страницы методов решения. Значения схем в выпадающих списках частично английские. |
| `Solution → Controls` | Все видимые рабочие подписи страницы переведены; числовые поля не изменялись. |
| `Solution → Initialization` | Переведены подписи и кнопки; инициализация не запускалась. |
| `Domain → Mesh…` / «Mesh Interfaces» | Переведены «Зоны», префикс и основные команды; имена зон сохранены. |
| `Setup → Dynamic Mesh` | Переведены основные подписи динамической сетки. Флажок включения не менялся. |
| `Setup → Motion Definitions` | Окно открылось с пустым списком; кнопки «Удалить», «Закрыть», «Справка» переведены. |
| `Setup → Curvilinear Coordinate System` | Частично переведены вкладки, параметры и команды; имена объектов и зон сохранены. |
| `Setup → Reference Frames` | Основные подписи, координаты, состояния и кнопки переведены. |
| `Setup → Cell Zone Conditions` | Заголовки и основные параметры переведены; зоны не редактировались. |
| `Setup → Boundary Conditions` | Основные подписи переведены; зоны не выбирались и не изменялись. |
| `Results → Graphics` | Заголовки страницы и команды настройки переведены. |
| `Results → Surfaces` | Переведены «Поверхности», фильтр и базовые команды; объекты не менялись. |

### Подтверждённые замечания для следующего блока

Это именно наблюдаемые английские строки в текущей установленной сборке, а не предположение о причине.

- `Quality`: `Evaluate Mesh Quality…`, `Improve Mesh Quality…`.
- `Mesh Interfaces`: `Mesh Interfaces` (заголовок и заголовок списка), `Options…`, `Fluid`, `Wall`.
- `Dynamic Mesh`: `In-Cylinder`; на части кнопок виден полный путь вида `Динамическая сетка/Настройки…`. Нужно проверить, является ли путь отображаемым целиком для всех состояний кнопки.
- `Motion Definitions`: `Manage Motion Definitions`, `Motion Definitions`, `New…`, `Edit…`.
- `Curvilinear Coordinate System`: `Name`, `Select Cell Zones`, `Cell Zones`, `Diffusion`, `Start and End`, `Display Face Zones`, `Visualize Direction Vectors`, `Auto Scale`, `Draw Mesh`, `Skip`.
- `Reference Frame`: `Reference Frame`, `Name`, `global`, `direction`.
- `Cell Zone Conditions` и `Boundary Conditions`: английские пункты дерева; на кнопках отображаются полные ключи с английским префиксом `Boundary Conditions/...`.
- `Results → Graphics`: `Mesh` и маркер `Unavailable`.
- `Results → Surfaces`: `Surface Type`, `Points`, `0D/1D/2D Facets`, `Rendering Priority`, `Medium`, `Group`, `UnGroup`, а также системные категории `Fluid`, `Internal`, `iso-surface`, `Point-surface`, `Wall`.
- `Domain → Display…`: системные категории `Fluid`, `Internal`, `iso-surface`, `Point-surface` остались английскими.
- `Solution → Methods`: видимые значения схем, в том числе `Coupled`, `Second Order`, `Second Order Upwind`, `Global Time Step`, остались английскими.

### Границы этого прохода

- Пустые списки критериев адаптации и определений движения зафиксированы только как визуальное состояние интерфейса. Их причина не устанавливалась.
- Маркеры `Unavailable` в графике не доказывают ошибку модели или причину недоступности. Сетка загружалась без файла данных решения; возможная связь с этим — только гипотеза.
- Проверка не включает настройку физической модели, граничных условий, материалов, выполнение инициализации, адаптации, расчёта или сохранение case.
- Имена пользовательских зон, поверхностей и объектов (`fluid_mrf`, `impeller`, `reference-frame-0` и т. п.) сохранялись без перевода.

### Очередность следующей работы

1. Перевести и собрать подтверждённые английские UI-строки выше, начиная с `Quality`, `Motion Definitions` и заголовков `Mesh Interfaces`.
2. Для полных ключей `Boundary Conditions/...` и `Динамическая сетка/...` сопоставить фактическое отображение с точными Qt `source`; не укорачивать исходный ключ.
3. Установить только новую сборку с отдельной резервной копией и повторно открыть только затронутые окна.
