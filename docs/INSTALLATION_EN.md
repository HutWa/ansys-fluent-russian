# Installation

This guide will be finalized after the Fluent 2026 R1 resource layout is verified on a local user installation.

The planned installer defaults to a preview (`--dry-run`), creates backups, and stores its manifest only with the backups. It must never delete original files.
